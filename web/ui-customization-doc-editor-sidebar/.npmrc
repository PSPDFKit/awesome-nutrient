# Refuse to resolve package versions published less than 15 days ago.
# Requires npm 11.10.1+; older npm warns "Unknown project config" and ignores
# this setting, and npm ci installs the locked versions regardless.
min-release-age=15

# Match the peer-dependency mode used to generate package-lock.json.
legacy-peer-deps=true
