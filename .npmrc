# Refuse to resolve package versions published less than 15 days ago.
# Requires npm 11.10.0+; older npm versions warn and ignore this setting.
min-release-age=15

# TODO(2026-08-18): every min-release-age-exclude entry in the example .npmrc
# files (and each minimumReleaseAgeExclude list in a pnpm-workspace.yaml) only
# exists because the pinned security-remediation versions were published inside
# the cooldown window; the youngest of them (next 16.3.0, published 2026-08-03)
# leaves the window on 2026-08-18. The js-yaml exclusions (4.3.1, published
# 2026-07-31, for CVE-2026-59870) leave the window on 2026-08-15. After
# 2026-08-18, remove the exclusions, relax the exact brace-expansion override
# pins (1.1.18 / 2.1.4 / 5.0.9) back to ranged floors, and collapse the
# stacked audit-generated override rules (the overlapping dompurify/minimatch/
# lodash ranges) into one rule per package.
